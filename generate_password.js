const showinfo = function (length, lowercase, uppercase, numbers, symbols, excludeCharacters) {
  return length + lowercase + uppercase + numbers + symbols + excludeCharacters;
}


module.exports = {
  showinfo
}